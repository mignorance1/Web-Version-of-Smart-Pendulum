# 精密单摆重力加速度测量实验平台 - 完整接口说明

## 项目概述

这是一个基于Web的精密单摆重力加速度测量实验平台，结合了计算机视觉技术与实时可视化。系统包含Python后端(Flask + OpenCV + WebSocket)和JavaScript前端(HTML5 + Canvas + WebSocket)。

---

## 目录
1. [后端代码结构分析](#后端代码结构分析)
2. [前端代码结构分析](#前端代码结构分析)
3. [前后端连接机制](#前后端连接机制)
4. [详细函数和变量说明](#详细函数和变量说明)
5. [API接口完整文档](#api接口完整文档)
6. [WebSocket消息格式](#websocket消息格式)

---

## 后端代码结构分析

### 🗂️ 文件结构
```
旧后端代码/
├── __main__.py              # 主程序入口
├── cam.py                   # 摄像头相关（未分析）
├── Pendulum/                # 核心功能包
│   ├── __init__.py          # 包初始化
│   ├── PendulumAPP.py       # 主应用类
│   ├── WebCore.py           # HTTP API服务器
│   ├── PendulumCore.py      # 单摆检测算法
│   ├── WebSocketServer.py    # WebSocket实时通信
│   ├── Exporter.py          # 数据导出和状态管理
│   ├── Logger.py            # 日志系统
│   ├── SerialConncet.py     # 串口通信
│   └── HelpUI.py            # 帮助界面（未分析）
```

### 📋 核心模块说明

#### 1. PendulumAPP.py - 主应用类

**类**: `App_Pendulum`

**主要功能**: 协调各个组件工作的主控制类

**关键变量**:
```python
self.detector: Detector_HSV     # HSV颜色检测器
self.analyzer: Analyzer         # 角度和位置分析器
self.exporter: Exporter         # 数据导出器
self.serial: Serial_Daemon      # 串口通信守护进程
self.cap: cv2.VideoCapture      # 摄像头对象
self._run_status: bool          # 运行状态标志
self.physicalLength: float      # 物理摆长(厘米)
self.ball_center: Optional[Tuple[int, int]]  # 小球中心位置
self.hsv_frame: Optional[np.ndarray]       # HSV图像帧
self.frame: Optional[np.ndarray]          # 原始图像帧
```

**主要函数**:

1. **`__init__(self, camera_source: int = 0)`**
   - **功能**: 初始化单摆应用
   - **参数**: `camera_source` - 摄像头索引
   - **变量**: 初始化各个组件，设置摄像头参数(1280x720, 20FPS)

2. **`setPhysicalLength(self, length_cm: float)`**
   - **功能**: 设置物理摆长
   - **参数**: `length_cm` - 摆长(厘米)
   - **作用**: 更新检测器的物理摆长参数

3. **`run(self)`**
   - **功能**: 启动单摆识别主循环
   - **流程**:
     - 启动串口线程
     - 主循环处理摄像头帧
     - 处理串口数据、Web事件、检测和分析
     - 更新帧率显示和绘制结果

4. **`_processSerialData(self)`**
   - **功能**: 处理串口数据，解析L开头的长度数据
   - **数据处理**:
     - 从`exporter.serial_raw_history`获取最新数据
     - 解析'L'前缀的长度数据
     - 验证数据有效性(丢弃>100的异常值)
     - 更新`PendulumState.physical_length`

5. **`_checkWebMouse(self)`**
   - **功能**: 处理网页端鼠标事件
   - **事件类型**: 左键点击、右键点击
   - **处理**: 转发到`analyzer.updateMouse`

6. **`_checkWebRoi(self, frame)`**
   - **功能**: 处理网页端ROI(感兴趣区域)设置
   - **参数**: `frame` - 当前图像帧
   - **验证**: 检查ROI格式、边界、最小尺寸
   - **处理**: 调用`detector.calibrate`进行HSV校准

7. **`_checkWebKey(self)`**
   - **功能**: 处理网页端按键事件
   - **队列**: 从`key_queue`和`exdata_queue`获取按键和附加数据
   - **转发**: 调用`_keyEventWeb`

8. **`_keyEventWeb(self, key: str, exdata: dict)`**
   - **功能**: 网页端按键事件处理
   - **支持的按键**:
     - 'q': 退出程序
     - 'p': 调试输出
     - 'c': ROI校准提示
     - 'z': 设置参考角度，发送信号到单片机
     - 'd': 删除参考点
     - 'l': 切换串口状态
     - 'x': 导出JSON数据
     - 'w/s/a': 电机控制(上/下/停止)
     - 'r/0': 清除数据
     - '1-8': 各种控制命令
     - 'a+angle': 设置角度

9. **`_drawn(self, frame)`**
   - **功能**: 绘制检测和分析结果到图像帧
   - **绘制内容**:
     - HSV阈值范围显示
     - 小球位置信息
     - HSV掩码小窗口
     - 小球中心点和轮廓
     - 摆线连接线
     - 角度、长度、摆轴信息

10. **`update_pendulum_data(self)`**
    - **功能**: 更新单摆状态数据
    - **更新内容**: 角度、位置、时间戳等

#### 2. WebCore.py - HTTP API服务器

**类**: `WebInterface`

**主要功能**: 提供HTTP API接口，处理网页请求

**关键变量**:
```python
self.app: Flask                   # Flask应用实例
self.host: str                    # 服务器主机地址
self.port: int                    # 服务器端口
self.cap: cv2.VideoCapture        # 摄像头对象
self.last_drawn_frame: np.ndarray # 最新绘制帧
self.key_queue: Queue[str]        # 按键事件队列
self.exdata_queue: Queue[dict]    # 附加数据队列
self.mouse_queue: Queue[dict]    # 鼠标事件队列
self.roi_queue: Queue[dict]      # ROI事件队列
self.paused_flag: threading.Event # 暂停标志
```

**主要函数和路由**:

1. **`__init__(self, host: str = "0.0.0.0", port: int = 5000)`**
   - **功能**: 初始化Web接口
   - **摄像头初始化**: 尝试多个摄像头索引(0,1,2)

2. **视频流接口**:
   ```python
   @self.app.route("/video_feed")
   def video_feed() -> Response:
       """视频流接口，返回MJPEG格式视频流"""
       return Response(self._gen_frames(), mimetype="multipart/x-mixed-replace; boundary=frame")
   ```

3. **控制接口**:
   ```python
   @self.app.route("/pendulum_key", methods=["POST"])
   def pendulum_key() -> tuple[str, int]:
       """接收按键事件"""

   @self.app.route("/pendulum_mouse", methods=["POST"])
   def pendulum_mouse() -> tuple[str, int]:
       """接收鼠标事件"""

   @self.app.route("/pendulum_roi", methods=["POST"])
   def pendulum_roi() -> Any:
       """接收ROI区域设置"""

   @self.app.route("/pendulum_pause", methods=["POST"])
   def pendulum_pause() -> tuple[str, int]:
       """暂停视频流"""

   @self.app.route("/pendulum_resume", methods=["POST"])
   def pendulum_resume() -> tuple[str, int]:
       """恢复视频流"""
   ```

4. **数据获取接口**:
   ```python
   @self.app.route("/get_pendulum_state")
   def get_pendulum_state() -> Any:
       """获取单摆当前状态"""
       return jsonify(PendulumState.getState())

   @self.app.route("/get_angle_history")
   def get_angle_history() -> Any:
       """获取角度历史记录"""

   @self.app.route("/get_position_history")
   def get_position_history() -> Any:
       """获取位置历史记录"""

   @self.app.route("/get_serial_log")
   def get_serial_log() -> Any:
       """获取串口日志"""
   ```

5. **参数配置接口**:
   ```python
   @self.app.route("/adjust_hsv", methods=["POST"])
   def adjust_hsv() -> Any:
       """调整HSV阈值"""

   @self.app.route("/hsv_calibration", methods=["POST"])
   def hsv_calibration() -> Any:
       """HSV校准接口"""
   ```

6. **`_gen_frames(self)`**
   - **功能**: 生成视频帧流
   - **控制**: 目标FPS 20，帧率控制
   - **处理**: 共享帧数据或直接摄像头读取

7. **`updateFrame(self, frame)`**
   - **功能**: 更新最新视频帧
   - **参数**: `frame` - 视频帧数据

#### 3. PendulumCore.py - 单摆检测算法

**主要类**:

##### Detector_HSV - HSV颜色检测器

**关键变量**:
```python
self.physicalLength: float      # 物理摆长
```

**主要函数**:

1. **`calibrate(self, hsv_frame, roi)`**
   - **功能**: HSV颜色校准
   - **参数**: `hsv_frame` - HSV图像帧, `roi` - ROI区域(x,y,w,h)
   - **算法**:
     - 计算ROI区域的HSV均值和标准差
     - 基于标准差计算容差(最小值保障)
     - 生成新的HSV阈值范围
     - 异常处理：使用默认阈值

2. **`getHsvMask(self, frame)`**
   - **功能**: 获取HSV掩码
   - **参数**: `frame` - 输入图像
   - **处理**: 转换为HSV，应用阈值，生成掩码

3. **`getBallCenter(self, mask_frame)`**
   - **功能**: 获取小球中心位置
   - **参数**: `mask_frame` - HSV掩码
   - **算法**:
     - 查找轮廓
     - 选择最大轮廓
     - 计算轮廓矩，获取中心点

##### Analyzer - 角度分析器

**关键变量**:
```python
self.PIVOT_POINT: Optional[Tuple[int, int]]           # 摆轴固定点
self.ORIGIN_POINT: Optional[Tuple[int, int]]          # 坐标原点
self.REFERENCE_ANGLE: Optional[float]                 # 参考角度
self.REFERENCE_VECTOR: Optional[Tuple[float, float]] # 参考向量
self.alpha: float                                    # 平滑系数
self.auto_pivot_enabled: bool                        # 自动摆轴检测开关
self.pivot_detection_history: list                   # 摆轴检测历史
self.pivot_confidence_threshold: float               # 摆轴检测置信度阈值
self.max_pivot_distance: int                         # 最大摆轴距离
```

**主要函数**:

1. **`updateMouse(self, event, x, y, flags, param)`**
   - **功能**: 鼠标事件更新
   - **左键**: 设置摆轴固定点
   - **右键**: 设置坐标原点

2. **`getAngleAndLine(self, frame, ball_center)`**
   - **功能**: 获取角度和摆线
   - **算法**:
     - 优先使用手动摆轴，其次自动检测摆轴
     - 计算摆线方向向量
     - 与参考向量计算夹角
     - 应用平滑滤波

3. **`autoDetectPivot(self, frame, ball_center)`**
   - **功能**: 自动检测摆轴位置
   - **算法**:
     - 边缘检测
     - 霍夫变换检测直线
     - 评分系统选择最佳摆轴
     - 历史记录平均化

4. **`setReferenceAngle(self, ball_center)`**
   - **功能**: 设置参考角度
   - **计算**: 基于当前摆线方向向量

5. **`cleanRef(self)`**
   - **功能**: 清除参考点和角度

#### 4. WebSocketServer.py - 实时通信

**全局变量**:
```python
CLIENTS: Set[websockets.WebSocketServerProtocol] = set()  # WebSocket客户端集合
angle_history: List[Dict[str, float]] = []                # 角度历史数据
position_history: List[Dict[str, float]] = []             # 位置历史数据
MAX_HISTORY_SIZE = 200                                    # 历史数据最大大小
hsv_lower = np.array([40, 70, 70])                        # HSV下限
hsv_upper = np.array([80, 255, 255])                      # HSV上限
server_running = False                                    # 服务器运行状态
server_error = None                                       # 服务器错误信息
```

**主要函数**:

1. **`register(websocket)`**
   - **功能**: 注册新的WebSocket客户端

2. **`unregister(websocket)`**
   - **功能**: 注销WebSocket客户端

3. **`process_message(websocket, message)`**
   - **功能**: 处理客户端消息
   - **支持的命令**: get_state, get_angle_history, get_position_history, get_log, get_serial_log, set_hsv, pendulum_key

4. **`send_pendulum_state(websocket=None)`**
   - **功能**: 发送单摆状态
   - **模式**: 单播或广播

5. **`send_angle_history(websocket=None)`**
   - **功能**: 发送角度历史数据

6. **`send_position_history(websocket=None)`**
   - **功能**: 发送位置历史数据

7. **`broadcast_loop()`**
   - **功能**: 定期广播数据循环
   - **频率**: 20Hz状态更新，1Hz历史数据

8. **`update_pendulum_data(frame)`**
   - **功能**: 处理摄像头帧，提取单摆数据
   - **输出**: 角度、位置、时间戳

#### 5. Exporter.py - 数据导出和状态管理

##### Exporter - 数据导出类

**类变量**:
```python
start_time: float = time.time()                    # 记录开始时间
angle_history: List[Tuple[float, Any]] = []       # 角度历史记录
position_history: List[Tuple[float, Any]] = []    # 位置历史记录
serial_history: List[Tuple[float, Any]] = []      # 串口数据历史
serial_raw_history: deque = deque(maxlen=1000)    # 原始串口数据缓冲区
```

**主要函数**:

1. **`recordAngle(angle, ntime)`**
   - **功能**: 记录角度数据
   - **参数**: `angle` - 角度值, `ntime` - 时间戳

2. **`recordPosition(position, ntime)`**
   - **功能**: 记录位置数据

3. **`recordSyncAP(angle, position, ntime)`**
   - **功能**: 同步记录角度和位置

4. **`recordSerial(serial, ntime)`**
   - **功能**: 记录串口数据

5. **`recordSerialRaw(raw)`**
   - **功能**: 记录原始串口数据

6. **`debug()`**
   - **功能**: 调试输出，显示最近10条记录

7. **`exportToJson(filename="exported.json")`**
   - **功能**: 导出数据为JSON文件

##### PendulumState - 单摆状态管理

**类变量**:
```python
hsv_lower: List[int] = [40, 70, 70]              # HSV下限
hsv_upper: List[int] = [80, 255, 255]            # HSV上限
state: Dict = {                                    # 状态字典
    "angle": 0.0,           # 单片机角度
    "camera_angle": 0.0,    # 摄像头识别角度
    "length": 100.0,
    "ball_x": 0.0,
    "ball_y": 0.0,
    "pivot_x": 0.0,
    "pivot_y": 0.0,
    "reference_angle": 0.0,
    "physical_length": 120.0,
    "timestamp": 0.0,
}
state_lock = threading.Lock()                      # 状态锁
```

**主要函数**:

1. **`setHsv(lower, upper)`**
   - **功能**: 设置HSV颜色阈值

2. **`update(...)`**
   - **功能**: 更新单摆状态
   - **参数**: 可选的各种状态参数
   - **特性**: 只更新非None的参数

3. **`getState()`**
   - **功能**: 获取当前状态副本

#### 6. Logger.py - 日志系统

**全局变量**:
```python
MAX_LOG_LENGTH: int = 500  # 最大日志长度
```

**装饰器**:
```python
@autoRegisterLogger
```

**Logger类**:

1. **`getLogs()`**
   - **功能**: 获取所有日志消息列表

#### 7. SerialConncet.py - 串口通信

##### Serial_PortManager - 串口管理器

**类变量**:
```python
_selected_port: str = "COM3"                     # 选择的串口
_ports: list[ListPortInfo] = []                 # 可用串口列表
```

**主要函数**:

1. **`listPorts()`**
   - **功能**: 列出所有可用串口
   - **返回**: 串口信息列表(设备名、描述、硬件ID)

2. **`selectFirstPort()`**
   - **功能**: 选择第一个可用串口

3. **`auto_select_CH340()`**
   - **功能**: 自动选择CH340设备

##### Serial_Daemon - 串口守护进程

**实例变量**:
```python
self.port: str                    # 串口号
self.baudrate: int                # 波特率
self.reconnect_interval: float    # 重连间隔
self.serial: serial.Serial | None # 串口对象
self._stop_event: threading.Event # 停止事件
self._thread: threading.Thread    # 守护线程
```

**主要函数**:

1. **`threadStart()`**
   - **功能**: 启动串口守护线程

2. **`threadStatus()`**
   - **功能**: 检查线程状态

3. **`_run()`**
   - **功能**: 线程运行体
   - **功能**: 持续尝试连接和接收串口数据

4. **`send(message: str)`**
   - **功能**: 发送数据到串口

5. **`changeStatus()`**
   - **功能**: 切换串口连接状态

#### 8. __main__.py - 主程序入口

**主要功能**:
```python
# 启动WebSocket服务器
websocket_server = start_server(host="127.0.0.1", port=5001)

# 启动串口
Serial_PortManager.auto_select_CH340()

# 启动主应用
app = App_Pendulum(0)
app.run()
```

---

## 前端代码结构分析

### 🗂️ 文件结构
```
前端代码/
├── index.html                    # 主页面
├── start-server.py               # 启动脚本
├── js/                          # JavaScript模块
│   ├── main.js                  # 应用入口
│   ├── websocket-client.js      # WebSocket客户端
│   ├── camera.js                # 摄像头管理
│   ├── pendulum.js              # 物理仿真
│   ├── chart.js                 # 数据可视化
│   ├── export.js                # 数据导出
│   └── ai-assistant.js          # AI助手
└── css/
    └── style.css                # 样式文件
```

### 📋 核心模块说明

#### 1. index.html - 主页面

**主要结构**:
- **头部**: 元数据、样式、脚本引用
- **主体**:
  - 控制面板(摄像头、串口、实验控制)
  - 数据显示区域(角度、长度、周期)
  - 图表区域(实时图表)
  - AI助手界面

**关键元素**:
```html
<!-- 视频流 -->
<img id="videoFeed" src="/video_feed" style="display:none">

<!-- 控制按钮 -->
<button id="startBtn">开始实验</button>
<button id="pauseBtn">暂停</button>
<button id="roiBtn">设置ROI</button>

<!-- 数据显示 -->
<div id="angleDisplay">角度: 0.0°</div>
<div id="lengthDisplay">长度: 120.0 cm</div>
<div id="periodDisplay">周期: 0.0 s</div>

<!-- Canvas元素 -->
<canvas id="pendulumCanvas" width="400" height="400"></canvas>
<canvas id="realtimeChart"></canvas>
```

#### 2. main.js - 应用入口

**主要功能**: 初始化应用，绑定事件，启动各个模块

**关键变量**:
```javascript
// 状态管理
let isRunning = false;              // 运行状态
let isPaused = false;               // 暂停状态
let experimentData = {             // 实验数据
    startTime: Date.now(),
    periods: [],
    gravity: 0,
    initialAngle: 5
};

// 模块实例
let cameraModule;                   // 摄像头模块
let pendulumModule;                // 单摆物理模块
let chartModule;                   // 图表模块
let wsModule;                      // WebSocket模块
let exportModule;                  // 导出模块
```

**主要函数**:

1. **`initApp()`**
   - **功能**: 初始化应用
   - **流程**:
     - 初始化各个模块
     - 绑定事件监听器
     - 启动WebSocket连接
     - 开始数据更新循环

2. **`bindEvents()`**
   - **功能**: 绑定用户界面事件
   - **事件**: 开始、暂停、ROI设置、参数调整等

3. **`updateDisplay()`**
   - **功能**: 更新数据显示
   - **更新内容**: 角度、长度、周期、重力等

#### 3. websocket-client.js - WebSocket客户端

**主要功能**: 处理WebSocket连接和消息通信

**关键变量**:
```javascript
const WS_URL = 'ws://127.0.0.1:5001';  // WebSocket服务器地址
let ws = null;                         // WebSocket对象
let reconnectAttempts = 0;             // 重连次数
const MAX_RECONNECT_ATTEMPTS = 5;     // 最大重连次数
const RECONNECT_DELAY = 2000;          // 重连延迟(毫秒)
```

**主要函数**:

1. **`connectWebSocket()`**
   - **功能**: 连接WebSocket服务器
   - **处理**: 自动重连机制

2. **`sendMessage(msg)`**
   - **功能**: 发送消息到服务器
   - **格式**: JSON格式

3. **`handleMessage(event)`**
   - **功能**: 处理接收到的消息
   - **消息类型**: pendulum_state, angle_history, position_history, logs

4. **`disconnectWebSocket()`**
   - **功能**: 断开WebSocket连接

#### 4. camera.js - 摄像头管理

**主要功能**: 管理摄像头视频流和ROI操作

**关键变量**:
```javascript
let videoFeed = document.getElementById('videoFeed');  // 视频元素
let roiCanvas = document.getElementById('roiCanvas');   // ROI画布
let isSelectingROI = false;                            // 是否正在选择ROI
let roiStart = { x: 0, y: 0 };                         // ROI起始点
let currentROI = null;                                 // 当前ROI
```

**主要函数**:

1. **`startVideoFeed()`**
   - **功能**: 启动视频流
   - **源**: HTTP API `/video_feed`

2. **`setupROISelection()`**
   - **功能**: 设置ROI选择模式
   - **交互**: 鼠标拖拽选择区域

3. **`sendROI(roi)`**
   - **功能**: 发送ROI到服务器
   - **API**: POST `/pendulum_roi`

4. **`adjustCameraSettings()`**
   - **功能**: 调整摄像头设置
   - **API**: POST `/adjust_hsv`

#### 5. pendulum.js - 物理仿真

**主要功能**: 单摆物理计算和仿真

**关键变量**:
```javascript
const g = 9.81;                         // 重力加速度
const canvas = document.getElementById('pendulumCanvas');  // 画布
const ctx = canvas.getContext('2d');    // 2D上下文
let pendulumState = {                   // 单摆状态
    angle: 0,                           // 角度(弧度)
    length: 1.0,                        // 摆长(米)
    angularVelocity: 0,                 // 角速度
    pivotX: 200,                        // 摆轴X坐标
    pivotY: 50,                         // 摆轴Y坐标
    ballRadius: 10                      // 小球半径
};
```

**主要函数**:

1. **`updatePhysics(deltaTime)`**
   - **功能**: 更新物理状态
   - **算法**: 基于单摆运动方程

2. **`drawPendulum()`**
   - **功能**: 绘制单摆动画
   - **绘制内容**: 摆线、小球、轨迹

3. **`calculatePeriod(angle)`**
   - **功能**: 计算周期
   - **公式**: 考虑大角度修正的单摆周期公式

4. **`calculateGravity(length, period)`**
   - **功能**: 计算重力加速度
   - **公式**: g = 4π²L/T²

#### 6. chart.js - 数据可视化

**主要功能**: 实时数据图表绘制

**关键变量**:
```javascript
let angleChart = null;                 // 角度图表
let positionChart = null;              // 位置图表;
let realtimeData = {                    // 实时数据
    angles: [],
    positions: [],
    timestamps: []
};
const MAX_DATA_POINTS = 100;           // 最大数据点数
```

**主要函数**:

1. **`initCharts()`**
   - **功能**: 初始化图表
   - **库**: Chart.js

2. **`updateRealtimeChart(data)`**
   - **功能**: 更新实时图表
   - **数据**: 角度、位置时间序列

3. **`updateAnalysisCharts(groups)`**
   - **功能**: 更新分析图表
   - **类型**: T-L关系、g-L关系、T-θ关系

4. **`createScatterChart(canvasId, data, xlabel, ylabel)`**
   - **功能**: 创建散点图
   - **特性**: 包含趋势线和理论曲线

#### 7. export.js - 数据导出

**主要功能**: 数据导出和处理

**关键变量**:
```javascript
const EXPERIMENT_GROUPS_KEY = 'experimentGroups';  // 实验数据存储键
let currentExperiment = {                           // 当前实验
    id: Date.now(),
    startTime: Date.now(),
    data: []
};
```

**主要函数**:

1. **`startExperiment(initialAngle, length)`**
   - **功能**: 开始新实验
   - **参数**: 初始角度、摆长

2. **`recordPeriod(duration, totalPeriods)`**
   - **功能**: 记录周期数据
   - **存储**: 添加到当前实验

3. **`finishExperiment()`**
   - **功能**: 完成实验
   - **计算**: 重力加速度、统计数据

4. **`exportToCSV(data, filename)`**
   - **功能**: 导出CSV格式

5. **`exportToJSON(data, filename)`**
   - **功能**: 导出JSON格式

6. **`exportToExcel(data, filename)`**
   - **功能**: 导出Excel格式

#### 8. ai-assistant.js - AI助手

**主要功能**: AI智能助手集成

**配置对象**:
```javascript
export const AIAssistant = {
    config: {
        apiKey: '填入你的apikey',
        baseUrl: 'https://open.bigmodel.cn/api/paas/v4/chat/completions',
        model: 'glm-4.5-flash'
    },
    // 其他方法...
};
```

**主要函数**:

1. **`init()`**
   - **功能**: 初始化AI助手
   - **流程**: 创建UI、绑定事件、加载知识库

2. **`loadKnowledgeBase()`**
   - **功能**: 加载实验知识库

3. **`callAIAPI(message)`**
   - **功能**: 调用AI API
   - **处理**: 消息格式化、响应处理

4. **`sendMessage()`**
   - **功能**: 发送用户消息
   - **特性**: 添加到聊天界面、显示加载状态

#### 9. utils.js - 工具函数

**主要功能**: 通用工具函数

**Utils对象**:
```javascript
export const Utils = {
    showToast(msg, duration = 2000) {
        // 显示toast提示
        let keyToast = document.getElementById('keyToast');
        if (!keyToast) {
            keyToast = document.createElement('div');
            keyToast.id = 'keyToast';
            document.body.appendChild(keyToast);
        }
        keyToast.textContent = msg;
        keyToast.style.display = 'block';
        clearTimeout(keyToast._timer);
        keyToast._timer = setTimeout(() => { keyToast.style.display = 'none'; }, duration);
    }
};
```

#### 10. style.css - 样式文件

**主要功能**: 应用程序样式定义

**关键特性**:
- **主题**: 蓝黑色渐变背景
- **响应式设计**: 适配不同屏幕尺寸
- **组件样式**: 按钮、面板、图表容器等
- **动画效果**: 过渡和悬停效果

---

## 前后端连接机制

### 🔗 连接架构

```
┌─────────────────┐    HTTP/MJPEG    ┌─────────────────┐
│   前端界面      │ ◄──────────────► │   WebCore.py    │
│  (index.html)   │    WebSocket     │   (Flask HTTP)  │
└─────────────────┘                  └─────────────────┘
       ▲                                       │
       │         WebSocket实时数据              │
       │         (端口5001)                    │
       ▼                                       │
┌─────────────────┐                           ┌─────────────────┐
│  实时图表      │                           │  WebSocketServer │
│  (Chart.js)    │                           │   (AsyncIO)    │
└─────────────────┘                           └─────────────────┘
```

### 📡 通信协议

#### 1. HTTP API (端口5000)

**请求类型**:
- **GET**: 获取数据、视频流
- **POST**: 发送控制命令、参数设置

**主要接口**:
```http
# 视频流
GET /video_feed
Content-Type: multipart/x-mixed-replace; boundary=frame

# 获取状态
GET /get_pendulum_state
Accept: application/json

# 发送按键事件
POST /pendulum_key
Content-Type: application/json

{
    "key": "z",
    "angle": 15.5
}

# 设置ROI
POST /pendulum_roi
Content-Type: application/json

{
    "x": 100,
    "y": 200,
    "w": 50,
    "h": 50
}

# 调整HSV
POST /adjust_hsv
Content-Type: application/json

{
    "lower": [40, 70, 70],
    "upper": [80, 255, 255]
}
```

#### 2. WebSocket API (端口5001)

**连接格式**:
```javascript
const ws = new WebSocket('ws://127.0.0.1:5001');
```

**消息格式**: JSON

**客户端发送消息**:
```json
{
    "command": "get_state",
    "extra": {
        "parameter": "value"
    }
}
```

**服务器发送消息**:
```json
// 状态更新消息
{
    "type": "pendulum_state",
    "data": {
        "angle": 15.3,
        "camera_angle": 15.3,
        "length": 100.0,
        "ball_x": 320,
        "ball_y": 240,
        "pivot_x": 320,
        "pivot_y": 50,
        "physical_length": 120.0,
        "timestamp": 1634567890000
    }
}

// 角度历史数据
{
    "type": "angle_history",
    "data": {
        "history": [
            {"time": 1634567890.1, "angle": 15.3},
            {"time": 1634567890.2, "angle": 14.8}
        ]
    }
}

// 日志消息
{
    "type": "logs",
    "data": {
        "log": [
            "2021-10-20 10:00:00 INFO: 启动单摆检测"
        ]
    }
}
```

### 🔧 数据流机制

#### 1. 摄像头数据流
```
摄像头 → OpenCV → WebCore._gen_frames() → HTTP/MJPEG → 前端视频元素
```

#### 2. 单摆检测数据流
```
摄像头帧 → PendulumCore.getHsvMask() → PendulumCore.getBallCenter()
    → Analyzer.getAngleAndLine() → PendulumState.update()
    → WebSocketServer.send_pendulum_state() → WebSocket客户端
```

#### 3. 用户控制数据流
```
前端界面 → HTTP POST/Pendulum_key/ROI → WebInterface.队列 → PendulumAPP._keyEventWeb()
```

#### 4. 串口数据流
```
串口 → Serial_Daemon._run() → 解析数据 → PendulumState.update() → WebSocket广播
```

---

## 详细函数和变量说明

### 后端函数详细说明

#### PendulumAPP.py

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `__init__` | `camera_source: int = 0` | None | 初始化应用，设置摄像头参数 |
| `setPhysicalLength` | `length_cm: float` | None | 设置物理摆长(厘米) |
| `run` | - | None | 主循环，协调各个组件工作 |
| `_processSerialData` | - | None | 处理串口数据，解析L前缀的长度信息 |
| `_checkWebMouse` | - | None | 处理网页鼠标事件队列 |
| `_checkWebRoi` | `frame` | None | 处理ROI设置请求，进行HSV校准 |
| `_checkWebKey` | - | None | 处理按键事件队列 |
| `_keyEventWeb` | `key: str`, `exdata: dict` | None | 处理各种按键命令 |
| `_drawn` | `frame` | None | 在图像上绘制检测结果 |
| `update_pendulum_data` | - | `dict` | 更新单摆状态数据 |

#### WebCore.py

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `__init__` | `host: str = "0.0.0.0"`, `port: int = 5000` | None | 初始化Flask应用和摄像头 |
| `_gen_frames` | - | generator | 生成MJPEG视频流 |
| `updateFrame` | `frame: np.ndarray` | None | 更新最新绘制帧 |
| `run_in_thread` | - | `Thread` | 在线程中启动Web服务 |

#### WebSocketServer.py

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `register` | `websocket` | None | 注册WebSocket客户端 |
| `unregister` | `websocket` | None | 注销WebSocket客户端 |
| `process_message` | `websocket`, `message` | None | 处理客户端消息 |
| `send_pendulum_state` | `websocket=None` | None | 发送单摆状态 |
| `update_pendulum_data` | `frame: np.ndarray` | `dict` | 处理摄像头帧数据 |

#### Exporter.py

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `recordAngle` | `angle`, `ntime` | None | 记录角度数据 |
| `recordPosition` | `position`, `ntime` | None | 记录位置数据 |
| `recordSyncAP` | `angle`, `position`, `ntime` | None | 同步记录角度和位置 |
| `recordSerialRaw` | `raw` | None | 记录原始串口数据 |
| `exportToJson` | `filename: str` | None | 导出数据为JSON |
| `setHsv` | `lower: np.ndarray`, `upper: np.ndarray` | None | 设置HSV阈值 |
| `update` | 多个可选参数 | None | 更新单摆状态 |
| `getState` | - | `dict` | 获取当前状态 |

#### SerialConncet.py

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `listPorts` | - | `list` | 列出可用串口 |
| `auto_select_CH340` | - | None | 自动选择CH340设备 |
| `threadStart` | - | None | 启动串口线程 |
| `send` | `message: str` | None | 发送数据到串口 |
| `_run` | - | None | 串口数据接收循环 |

### 前端函数详细说明

#### main.js

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `initApp` | - | None | 初始化应用模块 |
| `bindEvents` | - | None | 绑定用户界面事件 |
| `updateDisplay` | - | None | 更新数据显示 |

#### websocket-client.js

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `connectWebSocket` | - | None | 连接WebSocket服务器 |
| `sendMessage` | `msg: object` | None | 发送JSON消息 |
| `handleMessage` | `event` | None | 处理接收到的消息 |
| `reconnect` | - | None | 重连WebSocket |

#### camera.js

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `startVideoFeed` | - | Promise | 启动视频流 |
| `setupROISelection` | - | None | 设置ROI选择模式 |
| `sendROI` | `roi: object` | None | 发送ROI到服务器 |
| `adjustCameraSettings` | `settings: object` | None | 调整摄像头设置 |

#### pendulum.js

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `updatePhysics` | `deltaTime: number` | None | 更新物理状态 |
| `drawPendulum` | - | None | 绘制单摆动画 |
| `calculatePeriod` | `angle: number` | `number` | 计算单摆周期 |
| `calculateGravity` | `length: number`, `period: number` | `number` | 计算重力加速度 |

#### chart.js

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `initCharts` | - | None | 初始化图表 |
| `updateRealtimeChart` | `data: object` | None | 更新实时图表 |
| `updateAnalysisCharts` | `groups: array` | None | 更新分析图表 |

#### export.js

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `startExperiment` | `initialAngle: number`, `length: number` | None | 开始新实验 |
| `recordPeriod` | `duration: number`, `totalPeriods: number` | None | 记录周期数据 |
| `finishExperiment` | - | `object` | 完成实验并返回结果 |
| `exportToCSV` | `data: object`, `filename: string` | None | 导出CSV格式 |
| `exportToJSON` | `data: object`, `filename: string` | None | 导出JSON格式 |

#### ai-assistant.js

| 函数名 | 参数 | 返回值 | 功能说明 |
|--------|------|--------|----------|
| `init` | - | None | 初始化AI助手 |
| `loadKnowledgeBase` | - | None | 加载实验知识库 |
| `callAIAPI` | `message: string` | Promise | 调用AI API |
| `sendMessage` | - | None | 发送用户消息 |

---

## API接口完整文档

### HTTP API (端口5000)

#### 1. 视频流接口
```
GET /video_feed
```
**响应**: MJPEG视频流
**Content-Type**: multipart/x-mixed-replace; boundary=frame

#### 2. 状态获取接口
```
GET /get_pendulum_state
```
**响应**:
```json
{
    "angle": 15.3,
    "camera_angle": 15.3,
    "length": 100.0,
    "ball_x": 320,
    "ball_y": 240,
    "pivot_x": 320,
    "pivot_y": 50,
    "reference_angle": 0.0,
    "physical_length": 120.0,
    "timestamp": 1634567890000
}
```

#### 3. 控制接口
```
POST /pendulum_key
Content-Type: application/json

{
    "key": "z",
    "angle": 15.5
}
```

```
POST /pendulum_roi
Content-Type: application/json

{
    "x": 100,
    "y": 200,
    "w": 50,
    "h": 50
}
```

```
POST /pendulum_pause
```
```
POST /pendulum_resume
```

#### 4. 数据历史接口
```
GET /get_angle_history
```
**响应**:
```json
{
    "history": [
        {"time": 1634567890.1, "angle": 15.3},
        {"time": 1634567890.2, "angle": 14.8}
    ]
}
```

```
GET /get_position_history
```
```
GET /get_serial_log
```

#### 5. 参数设置接口
```
POST /adjust_hsv
Content-Type: application/json

{
    "lower": [40, 70, 70],
    "upper": [80, 255, 255]
}
```

```
POST /hsv_calibration
Content-Type: application/json

{
    "roi": {
        "x": 100,
        "y": 200,
        "w": 50,
        "h": 50
    }
}
```

### WebSocket API (端口5001)

#### 连接
```javascript
const ws = new WebSocket('ws://127.0.0.1:5001');
```

#### 客户端发送消息格式
```json
{
    "command": "get_state",           // 或 "get_angle_history", "get_position_history", "get_log", "get_serial_log"
    "extra": {
        "parameter": "value"          // 可选的额外参数
    }
}
```

#### 服务器发送消息类型

**1. 状态更新消息**
```json
{
    "type": "pendulum_state",
    "data": {
        "angle": 15.3,
        "camera_angle": 15.3,
        "length": 100.0,
        "ball_x": 320,
        "ball_y": 240,
        "pivot_x": 320,
        "pivot_y": 50,
        "physical_length": 120.0,
        "timestamp": 1634567890000
    }
}
```

**2. 角度历史数据**
```json
{
    "type": "angle_history",
    "data": {
        "history": [
            {"time": 1634567890.1, "angle": 15.3},
            {"time": 1634567890.2, "angle": 14.8}
        ]
    }
}
```

**3. 位置历史数据**
```json
{
    "type": "position_history",
    "data": {
        "history": [
            {"time": 1634567890.1, "x": 15.3, "y": -20.5},
            {"time": 1634567890.2, "x": 14.8, "y": -19.2}
        ]
    }
}
```

**4. 日志消息**
```json
{
    "type": "logs",
    "data": {
        "log": [
            "2021-10-20 10:00:00 INFO: 启动单摆检测",
            "2021-10-20 10:00:01 INFO: HSV校准成功"
        ]
    }
}
```

**5. 串口日志**
```json
{
    "type": "serial_logs",
    "data": {
        "log": [
            "L120.5",
            "角度: 15.3°",
            "周期: 2.1s"
        ]
    }
}
```

---

## WebSocket消息格式

### 消息类型总结

| 消息类型 | 频率 | 说明 | 前端处理 |
|----------|------|------|----------|
| `pendulum_state` | 20Hz | 实时单摆状态 | 更新显示、绘制动画 |
| `angle_history` | 1Hz | 角度历史数据 | 更新角度图表 |
| `position_history` | 1Hz | 位置历史数据 | 更新位置图表 |
| `logs` | 按需 | 应用程序日志 | 显示日志区域 |
| `serial_logs` | 按需 | 串口通信日志 | 显示串口数据 |

### 消息处理流程

1. **连接建立**:
   ```javascript
   ws.onopen = () => {
       console.log('WebSocket连接已建立');
       // 请求初始数据
       ws.send(JSON.stringify({command: 'get_state'}));
       ws.send(JSON.stringify({command: 'get_angle_history'}));
       ws.send(JSON.stringify({command: 'get_position_history'}));
   };
   ```

2. **消息处理**:
   ```javascript
   ws.onmessage = (event) => {
       const message = JSON.parse(event.data);
       switch(message.type) {
           case 'pendulum_state':
               updatePendulumDisplay(message.data);
               break;
           case 'angle_history':
               updateAngleChart(message.data.history);
               break;
           // 其他消息类型处理...
       }
   };
   ```

3. **错误处理**:
   ```javascript
   ws.onerror = (error) => {
       console.error('WebSocket错误:', error);
       // 尝试重连
       setTimeout(() => connectWebSocket(), RECONNECT_DELAY);
   };
   ```

---

## 系统启动流程

### 后端启动
```bash
# 启动主程序（自动启动HTTP和WebSocket服务器）
python -m 旧后端代码

# 启动顺序：
# 1. 启动WebSocket服务器 (端口5001)
# 2. 初始化串口通信
# 3. 启动主应用循环
# 4. 启动HTTP服务器 (端口5000)
```

### 前端启动
```bash
# 启动前端HTTP服务器
cd 前端代码
python -m http.server 8080

# 访问地址：http://localhost:8080
```

### 数据流时序
```
1. 摄像头启动 → 视频流传输 → 前端显示
2. 单摆检测 → 状态更新 → WebSocket推送 → 前端更新
3. 用户操作 → HTTP请求 → 后端处理 → 状态变更
4. 串口数据 → 解析处理 → 状态更新 → 实时推送
```

---

## 总结

这个精密单摆重力加速度测量实验平台具有以下特点：

1. **双通信机制**: HTTP API用于控制命令，WebSocket用于实时数据传输
2. **模块化设计**: 前后端分离，功能模块化，易于维护和扩展
3. **实时性**: 20Hz的实时数据更新，流畅的动画效果
4. **多种交互**: 支持鼠标、键盘、Web界面的多种操作方式
5. **数据可视化**: 丰富的图表和分析功能
6. **AI集成**: 智能助手提供实验指导和分析建议
7. **数据导出**: 支持多种格式的数据导出

通过详细的接口说明，开发者可以清楚地了解每个函数的作用、参数和返回值，以及前后端之间的数据流和通信机制，便于系统的维护、扩展和二次开发。